# Public Home Manager module flake

This is a public flake. It exports `homeManagerModules.default`, `devenvModules.default`, and `lib`. It does not wait to land in home-manager. Skills are a shared catalog in the same spirit as `programs.mcp`: declare once, agents inherit.
