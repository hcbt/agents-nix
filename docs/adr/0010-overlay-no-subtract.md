# Overlay adds or replaces; it does not subtract

An Agent may add or replace inherited MCP servers and skills by name; the Agent wins on clash. User instructions are a single file, so an Agent either replaces them or turns inheritance off. There is no exclude list in v1. Dropping one inherited server means turning MCP inheritance off on that Agent and declaring its list locally.
