# Catalog option types

A skills entry is a path or `{ path, subdir }`. A path whose root contains `SKILL.md` is a standalone skill; otherwise it is a pack (`subdir` defaults to `"skills"`). User instructions are `either lines path`, same as `programs.claude-code.context`; unset means the catalog is off. Which filename each agent writes is the agent adapter.
